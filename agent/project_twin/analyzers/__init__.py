"""Digital Twin language analyzers (PI-6).

Each analyzer exposes one coarse ``analyze`` operation and a capability/version manifest.
Analyzers are pure: stdlib parsing only, no SQLite/framework dependency.
"""
